<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlNormal">#FFFFFF</item>
        <item name="colorControlActivated">#FFFFFF</item>
        <item name="colorControlHighlight">@color/indigo</item>
        <item name="android:textColorHint">#FFFFFF</item>
        <item name="android:textColor">@color/black</item>

    </style>

    <style name="CustomPickerTheme">
        <item name="colorAccent">@color/background_all</item>
        <item name="colorControlActivated">@color/background_all</item>
        <item name="android:textColor">@color/background_all</item>
        <item name="colorControlHighlight">@color/indigo</item>
        <item name="colorControlNormal">@color/background_all</item>
    </style>

    <style name="AppTheme.Main" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">#4086c1</item>
    </style>

    <style name="MySwitch">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">@color/indigo</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">@color/normal_switch</item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">@color/grey</item>
    </style>

    <style name="CircularProgress" parent="Theme.AppCompat.Light">
        <item name="colorAccent">@color/indigo</item>
    </style>

    <style name="btnSign" parent="AppTheme.NoActionBar">
        <item name="android:width">150dp</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:radius">30dp</item>
        <item name="android:cursorVisible">true</item>
        <item name="android:background">@drawable/back_btn_sign</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="btnOtp">
        <item name="android:radius">30dp</item>
        <item name="android:background">#fff</item>
        <item name="android:textColor">#000000</item>
        <item name="android:text">@string/btnOtp</item>
    </style>

    <style name="popupTheme">
        <item name="android:textColor">@color/black</item>
        <item name="android:background">@color/white</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:background">@color/white</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <color name="textColorLogin">#FFFFFF</color>
    <string name="singIn">Sign In</string>


    <style name="error_validating" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/red_validate</item>
        <item name="android:textColorHint">@color/red_validate</item>
    </style>
</resources>
